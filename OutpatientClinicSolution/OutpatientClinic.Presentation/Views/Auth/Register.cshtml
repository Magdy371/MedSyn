@model OutpatientClinic.Core.DTOS.RegisterModel

@{
    ViewData["Title"] = "Register";
}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cliniva - Register</title>
    <link rel="stylesheet" href="~/css/register.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<div class="container-fluid d-flex align-items-center justify-content-center min-vh-100">
    <div class="signup-container shadow-lg">
        <div class="left-section">
            <img src="~/images/heart_illustration.png" alt="Clinic Logo" class="logo">
            <img src="~/images/medical-team.png" alt="Doctors Image" class="hero-img">
        </div>
        <div class="right-section">

            <h3 class="text-center">Register</h3>
            <p class="text-center">Enter your info to create an account</p>

            <form asp-action="Register" asp-controller="Auth" method="post">

                <!-- Username Field -->
                <div class="mb-3">
                    <label asp-for="Username" class="form-label">Username</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-user"></i></span>
                        <input asp-for="Username" class="form-control" id="Username" required>
                    </div>
                    <span asp-validation-for="Username" class="text-danger"></span>
                    <!-- Add uniqueness error -->
                    @if (ViewData.ModelState["Username"]?.Errors.Any(e => e.ErrorMessage == "Username is already taken.") == true)
                    {
                        <span class="text-danger">Username is already taken.</span>
                    }
                </div>

                <!-- Email Field -->
                <div class="mb-3">
                    <label asp-for="Email" class="form-label">Email</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                        <input asp-for="Email" class="form-control" id="Email" required>
                    </div>
                    <span asp-validation-for="Email" class="text-danger"></span>
                    <!-- Add uniqueness error -->
                    @if (ViewData.ModelState["Email"]?.Errors.Any(e => e.ErrorMessage == "Email is already registered.") == true)
                    {
                        <span class="text-danger">Email is already registered.</span>
                    }
                </div>

                <!-- Phone Number Field -->
                <div class="mb-3">
                    <label asp-for="PhoneNumber" class="form-label">Phone Number</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-phone"></i></span>
                        <input asp-for="PhoneNumber" class="form-control" id="PhoneNumber" required>
                    </div>
                    <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                    <!-- Add uniqueness error -->
                    @if (ViewData.ModelState["PhoneNumber"]?.Errors.Any(e => e.ErrorMessage == "Phone number is already in use.") == true)
                    {
                        <span class="text-danger">Phone number is already in use.</span>
                    }
                </div>

                <div class="mb-3">
                    <label asp-for="FullName" class="form-label">Full Name</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-user"></i></span>
                        <input asp-for="FullName" class="form-control" id="FullName" required>
                    </div>
                    <span asp-validation-for="FullName" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Password" class="form-label">Password</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-lock"></i></span>
                        <input asp-for="Password" class="form-control" id="Password" required>
                    </div>
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Role" class="form-label">Role*</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-user-tag"></i></span>
                        <select asp-for="Role" class="form-control" id="Role" required>
                            <option value="">Select Role</option>
                            <option value="Admin">Admin</option>
                            <option value="Doctor">Doctor</option>
                            <option value="Patient">Patient</option>
                            <option value="Staff">Staff</option>
                            <!-- Add new roles below -->
                            <option value="Receptionist">Receptionist</option>
                            <option value="Nurse">Nurse</option>
                            <option value="Technical_Support">Technical Support</option>
                        </select>
                    </div>
                    <span asp-validation-for="Role" class="text-danger"></span>
                </div>

                <button type="submit" class="btn btn-primary w-100">Register</button>

            </form>
        </div>
    </div>
</div>
